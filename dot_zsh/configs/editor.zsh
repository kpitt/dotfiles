# Set the default editor.
# Don't set this to `gvim` or `mvim`, it won't work.
# Don't set this to `gvim -f`, it won't work (unless you wrap it in a script).
# http://robots.thoughtbot.com/visual-ize-the-future
export VISUAL=nvim
export EDITOR=$VISUAL
