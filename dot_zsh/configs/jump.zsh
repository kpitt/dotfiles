# Initialize "gsamokovarov/jump" fuzzy cd utility.
# This is made availabe for evaluation, but is currently not enabled.
# if [[ -x "$(command -v jump)" ]]; then
#     _evalcache jump "$(jump shell zsh)"
# fi
