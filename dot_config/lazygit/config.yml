gui:
  showCommandLog: false
  theme:
    selectedLineBgColor: [ "#313752" ]
  nerdFontsVersion: "3"

git:
  autoFetch: false
  pagers:
    - pager: delta --dark --paging=never --line-numbers --hyperlinks --hyperlinks-file-link-format="lazygit-edit://{path}:{line}"

os:
  editPreset: "nvim"

refresher:
  refreshInterval: 15
